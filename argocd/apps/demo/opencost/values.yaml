opencost:
  metrics:
    kubeStateMetrics:
      emitNamespaceAnnotations: true
      emitPodAnnotations: true
  ui:
    ingress:
      enabled: true
      ingressClassName: "nginx"
      annotations:
        kubernetes.io/ingress.class: nginx
        kubernetes.io/tls-acme: "true"
        cert-manager.io/cluster-issuer: "letsencrypt-demo"
      hosts:
        - host: demo.infra.opencost.io
          paths:
            - /
      tls:
        - secretName: opencost-tls
          hosts:
            - demo.infra.opencost.io
    image:
      fullImageName: ghcr.io/opencost/opencost-ui:develop-f999292
  prometheus:
    internal:
      serviceName: prometheus-kube-prometheus-prometheus
      port: 9090
  exporter:
    prometheusDataSource:
      queryResolutionSeconds: "60"
    image:
      fullImageName: ghcr.io/opencost/opencost:develop-30490a1
