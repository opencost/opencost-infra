opencost:
  metrics:
    kubeStateMetrics:
      emitNamespaceAnnotations: true
      emitPodAnnotations: true
  ui:
    ingress:
      enabled: true
      ingressClassName: "nginx"
      annotations:
        kubernetes.io/ingress.class: nginx
        kubernetes.io/tls-acme: "true"
        cert-manager.io/cluster-issuer: "letsencrypt-demo"
      hosts:
        - host: demo.infra.opencost.io
          paths:
            - /
      tls:
        - secretName: opencost-tls
          hosts:
            - demo.infra.opencost.io
    image:
      fullImageName: ghcr.io/opencost/opencost-ui:develop-5120a90
  prometheus:
    internal:
      serviceName: prometheus-kube-prometheus-prometheus
      port: 9090
  exporter:
    image:
      fullImageName: ghcr.io/opencost/opencost:develop-faff1c3
    prometheusDataSource:
      queryResolutionSeconds: "60"
